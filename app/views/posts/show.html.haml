%h3= @post.title
%p= "by #{@post.user.username} on #{@post.created_at.strftime("%B %d %Y %X %p")}"
%p= raw(@post.content)

%a= @post.category

= link_to 'Edit', edit_post_path(@post)
\|
= link_to 'Back', posts_path
